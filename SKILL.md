---
name: Record
description: 在用户说“使用 Record skill”“Record 一下”“记录一下”或“整理记录一下”时使用。维护项目根目录唯一的 Record.md，记录项目阶段、事实、知识卡点、高频问题、反复纠正与返工原因、我的闪光点，并在用户要求时根据已有记录生成小红书图文选题或视频大纲。用户主动决定何时记录、项目是否结束以及记录哪些阶段。
---

# Record

## 核心边界

- 只维护项目根目录的一个 `Record.md`，禁止在子文件夹新建记录文件，除非用户明确指定。
- 用户主动触发记录。不要自行判断项目结束，也不要在没有触发词时擅自追加记录。
- 记录必须来自当前对话、项目文件或用户明确提供的信息；不编造日期、结果、指标、责任边界或用户观点。
- 默认使用简体中文。保留用户有辨识度的原话或短语，避免把记录改写成空泛的 AI 总结。
- 每次整理前先读取已有 `Record.md`，检查重复、遗漏和矛盾。

## 触发与模式识别

识别以下表达为记录触发：`使用 Record skill`、`Record 一下`、`记录一下`、`整理记录一下`，以及语义明确的同类说法。

触发后判断用户意图：

1. **阶段记录**：整理当前节点的新事实和有价值的事情，并更新对应阶段。
2. **校正记录**：发现旧内容可能错误或过时。先向用户指出具体内容并询问是否修改；得到确认后直接修改。未确认前不改动该事实。
3. **选题输出**：用户要求“根据 Record.md 做选题/输出”时，读取完整记录，按要求生成选题或大纲，不因项目未结束而拒绝。

## 价值筛选

优先记录以下三类内容，并写清“发生了什么、为什么值得记录、以后怎么用”：

- 用户原本无法解决、需要 AI 帮助捋清的知识点。
- 项目中被反复询问、反复确认，或直接构成项目难点的问题。
- 用户经常纠正 AI、反复调整或返工的内容，并总结更好的描述方式或协作约定。
- 用户在本阶段的闪光点：最有成就感、最值得骄傲、体现学习突破、工作流搭建能力或别出心裁创意的事情。

不要为了填满模板记录普通进展。无法证明有价值的内容放入简短事实记录，不放入“有价值的事情”。

## Record.md 固定结构

维护以下顺序；没有内容的部分保留标题并写“暂无”。

````markdown
# [项目名称]

## 项目阶段总览
- 开始日期：[YYYY-MM-DD 或待补充]
- 结束日期：[用户说明的日期 / 未说明]

```mermaid
flowchart TB
  ROOT[项目名称]
  ROOT --> S1[阶段一：名称｜成果]
  S1 --> S2[阶段二：名称｜成果]
````

## 阶段一：[名称]｜[YYYY-MM-DD 至 YYYY-MM-DD/进行中]
### 事实记录
### 有价值的事情
#### 知识卡点
#### 高频问题
#### AI 协作纠正与返工
#### 我的闪光点
### 阶段选题推荐
- [选题]

## 整个项目总结
### 项目记录
> 本部分作为项目描述和介绍使用，完整说明项目为什么做、做了什么、结果如何，方便复盘或直接复制介绍项目。
### 最核心的有价值事情
#### 最重要的闪光点
### 全项目选题推荐

## 选题推荐汇总
### 用户指定的选题
- [ ] [选题]
### 整个项目选题
- [ ] [选题]
### 各阶段 AI 推荐
- [ ] [阶段：选题]
````

具体执行时，根据实际阶段数量增删阶段标题，但保持总览、阶段记录、整个项目总结、选题汇总的顺序。

## 日期与事实修正

- 只能使用用户明确说过或项目文件可核实的日期。无法确认时写“实际日期待补充；本次整理日期：YYYY-MM-DD”。
- 用户未说明结束日期时写“未说明”，不要把最后一次对话日期当作结束日期。
- 发现旧记录有错误、过时或与新证据冲突时，先列出待修正内容并询问用户。用户确认后直接更新原文；无需额外保留修订日志，除非用户要求。

## 阶段选题规则

- 每个阶段推荐 3 至 5 个选题，只写选题标题，不展开正文。
- 当前阶段选题以当前阶段为主，可少量结合此前阶段；不得引用未来阶段。
- 默认当前阶段相关选题数量多于跨阶段选题数量。
- 用户指定的选题或主题完成后，追加到文件末尾“选题推荐汇总”的最前面。

## 用户要求输出内容时

- 只要求选题：输出分组后的选题，不自动扩写。
- 要求小红书图文：先给选题，再按用户指定选题输出可直接修改的图文草稿；保留用户原话、具体过程和真实结果。
- 要求视频：输出标题、开头钩子、内容段落和结尾行动，不虚构镜头、数据或经历。
- 若用户未指定形式，默认偏向小红书图文，并先输出选题供用户选择。

## Mermaid 说明

默认在项目总览使用 Mermaid `flowchart TB`。节点只写阶段名称、阶段成果和关键步骤/价值点，保持简短。用户可以要求改为纯文字。可用 Mermaid Live Editor（https://mermaid-live.nodejs.cn/）或 jyshare（https://www.jyshare.com/front-end/9729/）查看、修改和导出图表。

## 完成检查

更新或输出前确认：

- 只存在或更新项目根目录的 `Record.md`。
- 新事实都有阶段和实际日期；不确定日期已标记待补充。
- 价值记录能对应三类价值之一，且保留了用户表达或具体证据。
- “我的闪光点”单独记录，并在项目总结中提炼最重要的闪光点。
- 阶段选题为 3 至 5 个，未引用未来阶段。
- 用户指定选题位于选题汇总最前面。
